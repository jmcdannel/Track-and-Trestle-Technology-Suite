{
  "name": "trestle-tt-suite",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.1.1"
  },
  "description": "Monorepo of apps and apis for the Trestle TT Suite",
  "version": "0.0.1",
  "main": "index.mts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package:layout-api": "lerna run --scope @trestle-tt/trestle-tt-layout-api --stream dev",
    "start": "lerna run start --stream"
  },
  "author": "Josh McDannel",
  "license": "MIT",
  "dependencies": {
    "wait-on": "^7.0.1"
  }
}
